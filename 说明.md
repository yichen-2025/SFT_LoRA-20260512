# 需要的核心第三方库
- torch
- transformers
- peft
- datasets

# 安装方式
在终端（如命令提示符，CMD）输入
```bash
pip install 第三方库名
```
可以安装大部分Python第三方库

> torch库需要安装GPU版本，安装过程可能有些复杂，可以在Pytorch官网（https://pytorch.org）找到安装的命令行
